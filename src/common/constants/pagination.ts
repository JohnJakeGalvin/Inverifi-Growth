export const PAGE_SIZE = 9;

// Invotra GraphQL backend returns 10 items by default and up to 50 items.
// Ask for 1000 items and let backend return as much entries as it's allowed to.
export const AS_MUCH_AS_POSSIBLE = 1000;
